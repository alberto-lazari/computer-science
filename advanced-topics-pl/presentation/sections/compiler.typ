#import "/common.typ": *

#new-section("Compiler")

#slide[
  Steps to compile source files to PDF:
  #uncover(1, mode: "transparent")[
    + Parsing
  ]
  #uncover("1-2")[
    2. Evaluation
  ]
  #uncover(1, mode: "transparent")[
    3. Lifting
    +  Layout
    +  Export
  ]
]
